-- Demo note: the Expo app ships with fixtures/seed JSON and works without this DB seed.
-- When using local Supabase, load destination fixtures via your app sync or insert manually.
select 'Gay-i seed placeholder — use fixtures/seed/destinations.json for MVP demo data' as notice;
